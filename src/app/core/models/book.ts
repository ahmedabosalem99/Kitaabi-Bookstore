export interface Book {
  id: string;
  bookName: string;
  authorName: string;
  price: string;
  quantity: string;
  description: string;
  imageURL: string;
  isApproved: boolean;
  categoryId:string;
}
